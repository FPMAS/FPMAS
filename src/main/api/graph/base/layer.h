#ifndef LAYER_API_H
#define LAYER_API_H

#include <vector>
#include "arc.h"

//namespace FPMAS::api::graph::base {
	//template<typename, typename, typename> class Node;

	/**
	 * Type used to index layers.
	 */
	//typedef int LayerId;

	/**
	 * Index of the layer used by default.
	 */
	//static constexpr LayerId DefaultLayer = 0;

	//template<typename node_type, typename arc_type>
	//class Layer {
		//public:
			//virtual std::vector<arc_type*> getIncomingArcs() const = 0;
			//std::vector<node_type*> inNeighbors() const;

			//virtual std::vector<arc_type*> getOutgoingArcs() const = 0;
			//std::vector<node_type*> outNeighbors() const;
	//};
/*}*/
#endif
